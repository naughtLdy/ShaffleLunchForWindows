<Window x:Class="ShuffleLunch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShuffleLunch"
		xmlns:vm="clr-namespace:ShuffleLunch.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}"
		Height="350"
		Width="525">

    <Window.DataContext>
        <vm:WindowViewModel />
    </Window.DataContext>
	
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Menu IsMainMenu="True">
                <MenuItem Header="File">
                    <MenuItem Header="Open" Command="{Binding FileOpen}"/>
                </MenuItem>
            </Menu>
        </Grid>

        <Grid Grid.Row="1">
            <ListBox ItemsSource="{Binding Path=PersonAndDeskList}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Image Grid.Column="0" Source="{Binding Path=image}" />
                            <TextBlock Grid.Column="1" Text="{Binding Path=name}" />
                            <ComboBox Grid.Column="3"
									  ItemsSource="{Binding Path=desk}"
									  SelectedIndex="0">
                            </ComboBox>
                        </Grid>
					</DataTemplate>
				</ListBox.ItemTemplate>
				
			</ListBox>
		</Grid>
    </Grid>
</Window>
